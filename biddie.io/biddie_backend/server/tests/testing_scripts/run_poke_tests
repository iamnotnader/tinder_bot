export PYTHONPATH=$PYTHONPATH:~/Development/biddie_backend/

if [ "$#" -ne 0 ]; then
    python -m unittest server.tests.poke_tests.PokesTestCase.$1
else
    python -m unittest --failfast server.tests.poke_tests.PokesTestCase
fi
