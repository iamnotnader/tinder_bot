export PYTHONPATH=$PYTHONPATH:~/Development/biddie_backend/

if [ "$#" -ne 0 ]; then
    python -m unittest server.tests.leave_review_tests.LeaveReviewTestCase.$1
else
    python -m unittest --failfast server.tests.leave_review_tests.LeaveReviewTestCase
fi
